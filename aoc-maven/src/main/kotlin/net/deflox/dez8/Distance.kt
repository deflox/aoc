package org.example.net.deflox.dez8

class Distance(val from: Point, val to: Point, val distance: Double) {
    override fun toString(): String {
        return "Distance(from=$from, to=$to, distance=$distance)"
    }
}